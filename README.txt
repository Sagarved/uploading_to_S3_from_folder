This is a script to push raw files to S3 bucket

Folder will handle raw file when upload completed. Raw files moved to "Wireshark Logs" folder.

This script run through cronjob and timing is configured with sudo user.

This runs on Python3.

add script kickoff timing to  crontab -e 

